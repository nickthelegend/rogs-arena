# Rogs Arena — Test Plan (zero tolerance)

The checklist every verification run is measured against. **PASS** means the real result matches the
"Correct means" column *exactly*, on the real deployment, with **zero console errors and zero failed
network requests** for UI items. Anything else is **FAIL**. Items that need a dependency we don't have
are **UNTESTABLE** with the exact reason. Nothing is marked PASS by analogy.

Environments:
- **Chain:** Solana devnet + MagicBlock devnet-as ER. Program `J83qUBtZwGwgyA7Sta8Kbj1GTTA6qtUBLEnkDV8wA64q`, arena PDA `ApzYL11HC9puv4dbFk1QTCrE4wpLup8ta9QE2UKde2CJ`.
- **Backend:** Railway `https://arena-production-0bdd.up.railway.app` (MongoDB Atlas DB `rogs_arena`).
- **Frontend:** Vercel `https://rogs-arena.vercel.app`. It serves the 00:09 UTC build; every CLI deploy since 00:24 UTC is stuck in `UNKNOWN` on Vercel (no build, no logs). Fixes after that build are verified on a local production build (`next build` + `next start` of the same commit, with the Vercel production env) against the same live Railway service, devnet program and MagicBlock ER, and each row names which one it used.
- **Browser:** Claude in Chrome (real Chrome). Console + network are checked on every UI item.

Methods: **B** = real browser against the deployed app. **S** = real signed devnet transactions from
`packages/arena-sdk/scripts/*` (for program paths the UI deliberately never exposes). **H** = real HTTP/WS
request against the deployed service, observed in the browser network tab or from a script.

Status legend: `NOT RUN` · `PASS` · `FAIL` · `UNTESTABLE (reason)`.

---

## 1. On-chain program (MagicBlock ER + Solana)

| ID | Item | Method | Steps | Correct means | Status |
|---|---|---|---|---|---|
| CH-01 | Program deployed | S | `solana program show J83q…` on devnet | Upgradeable program, 654,792 bytes, authority `Ens1TxKQ…` | PASS — upgradeable, 654,792 bytes, authority Ens1TxKQ… (05:40) |
| CH-02 | Arena delegated | S | Read the arena account on base and ER | Base owner = `DELeGG…`; ER owner = program; router `getDelegationStatus` isDelegated=true | PASS — base owner DELeGG…, ER owner program, router isDelegated=true fqdn devnet-as (05:40) |
| CH-03 | Crank rolls rounds without a keeper | S | `watch-crank.ts` with keeper disabled | `current.id` increments and `last_roll_ts - previous end_ts ≤ 5s`; outcome is YES iff close ≥ strike | PASS — round 1 → 2 rolled 1s after end_ts with no keeper; YES with close 7726266920786 ≥ strike 7725570971918 (watch-crank.ts) |
| CH-04 | Strike and close come from the MagicBlock oracle | S | Compare `RoundOpened.strike_price` / `RoundResolved.close_price` with the BTC/USD feed `71wtT…` at roll time | Both equal the feed's `price` at a `publish_time` within 10s of `last_roll_ts` | PASS — round 5 strike 7724071556152 = feed price published at 1789255801 (roll at 1789255801); round 4 close equals the same feed value; 1,970 live updates sampled |
| CH-05 | init_player + delegate_player | S+B | Fresh wallet → setup | Player PDA exists, base owner `DELeGG…`, visible on the ER with owner = program | S PASS (E2E-RUN P1/P2 init+delegate) · B PASS (UI guest FTBkigFu…: Player BnN4iyxy… base owner DELeGG…, router isDelegated=true) |
| CH-06 | Session key (Gum V2) authorises ER actions | S+B | `createSessionV2`, then `claim_chips` signed only by the session key | Tx succeeds on the ER; the session token PDA `["session_token_v2", program, signer, authority]` exists on base | S PASS (claim_chips signed only by the session key, 281ms) · B PASS (UI: CreateSessionV2 5kdUvZNc…; buy and sell fee payer is session key E3mDMV…, fee 0) |
| CH-07 | Wrong session / non-owner rejected | S | Session key of wallet A signs `buy` for wallet B's player; random key with no token signs `buy` | Both fail: `InvalidToken` / `Unauthorized`; balances unchanged | PASS — "Invalid session token" and "Signer is not allowed to act for this account"; balances unchanged (TEST-RUN-CHAIN) |
| CH-08 | claim_chips first join | S+B | New player claims | Balance = 250.000000; `joined=true`; `ChipsClaimed{first:true}` | S PASS (balance 250.000000 after the first claim) · B PASS (UI: ER balance 250, joined=true right after setup) |
| CH-09 | claim_chips faucet guard | S | Claim again immediately | Fails `FaucetCooldown` (or `FaucetBalanceTooHigh`); balance unchanged | PASS — "Faucet is cooling down", balance unchanged |
| CH-10 | buy happy path | S+B | Buy YES $5 | Balance −5; shares = SDK `quoteBuy` exactly; `TradeExecuted` side 0; yes_price moves up | S PASS (shares 9780446 = quote; balance 245.000000) · B PASS (UI Buy UP $5: balance 250 -> 245, 9.780446 YES, TradeExecuted BUY sig 5Wg1QxjU… indexed) |
| CH-11 | buy limits | S | amount 0.5 USD; 101 USD; > balance | `AmountTooSmall`; `AmountTooLarge`; `InsufficientBalance` | PASS — below minimum / above maximum / "Not enough chips" with state unchanged |
| CH-12 | buy slippage | S | `min_shares = quote + 1` | `SlippageExceeded`; no state change | PASS — "Price moved beyond the allowed slippage", state unchanged |
| CH-13 | trading lock | S | Buy in the last 5s of a round | `TradingLocked` | PASS — buy at end_ts-3 → "Trading is locked in the final seconds of the round" |
| CH-14 | sell happy path | S+B | Sell half of a position | Balance + `quoteSell.out`; shares reduced; `TradeExecuted` side 1 with `realized_pnl` | S PASS (sold 5009741 shares for 2.465775; balance exact) · B PASS (UI TP: SELL 4uGdMzDF… proceeds 4.9005, realized -0.0995) |
| CH-15 | sell more than held | S | Sell held+1 | `NothingToSell` | PASS — "Not enough shares to sell" |
| CH-16 | attach_ability rules | S+B | Attach before lock; second card; after a sell; within 30s of end | First OK (`AbilityAttached`); others `AbilityAlreadySet` / `TradingLocked` | S PASS (attach OK tx JXRbJ8Z2…; second card, after-sell and last-30s rejected) · B PASS (UI attach_ability in 239 ms; second card rejected with the program message) |
| CH-17 | report_heart | S | bpm 80 → OK; bpm 20 → fail | `heart_bpm=80` and the active position's `max_bpm` updated; bpm 20 fails `InvalidHeartRate` | PASS — bpm 80 → heart 80, maxBpm 80; bpm 20 → "Heart rate is out of range" |
| CH-18 | Settlement math | S | Settle after resolution | balance delta = payout + bonus from SDK `settleSlot`; slot cleared; wins/losses/streak updated | PASS — E2E P1 delta 19.780446 = payout 9.780446 + bonus 10; P2 loss delta 0; stats updated |
| CH-19 | Double / Protect / Calm bonuses | S | Positions with each card | Double = min(profit,10); Protect = min(loss,10); Calm = 10 only if max_bpm<120 with samples | PASS (Run 3, 05:40 UTC, keeper-race-safe script): Double Profit profit 1.104787 → bonus 1.104787, balance delta 1.104787; Protect Loss profit -1.862707 → bonus 1.862707, balance delta 1.862707; both settled by the Railway keeper (9PrnWiJP…, 2YUZ4iLL…) and read from its PositionSettled events. Run 2 FAILED on the keeper race — fixed in scripts/lib/settlement.ts |
| CH-20 | Cheers via VRF | S | `e2e-cheers-vrf.ts` | `cheers_pending=1` after settle; `request_cheers` succeeds; callback pays each candidate exactly 1.000000; `CheersPaid` includes the randomness | PASS — cheers_pending=1; request_cheers 3z18MVog…; callback 3acK2K49… paid +1.000000; CheersPaid decoded with 32-byte randomness (after the SDK parser fix) |
| CH-21 | settle idempotent | S | `settle_player` twice | Second call succeeds with no balance change and no event | PASS — two settle_player calls, balance unchanged at 248 |
| CH-22 | commit_player / commit_arena | S | Commit, then read base | Base data equals ER state (trades, balance / round, commits) while still delegated | PASS — commit_player: base shows trades 1, balance 264.780446; commit_arena: base went round 0 → 2, trades 0 → 6, still delegated |
| CH-23 | Authority-only instructions | S | `fund_treasury` / `commit_arena` signed by a random key | `Unauthorized` | PASS — fund_treasury and commit_arena by a stranger both rejected |
| CH-24 | Rust unit tests | S | `cargo test -p rogs-arena --lib` | 22 passed, 0 failed | PASS — 22 passed, 0 failed (fresh rerun 05:43) |
| CH-25 | SDK tests | S | `bun test` in packages/arena-sdk | All pass, including the live oracle read | PASS — 13/13 bun tests including the live oracle read and real CPI event logs |

## 2. Arena service HTTP API (Railway)

| ID | Endpoint / case | Method | Correct means | Status |
|---|---|---|---|---|
| API-01 | `GET /health` | H | 200 `{ok:true, mongo:true, er:true, programId:"J83q…", arena:{roundId>0, status:"open", endTs equal to the ER arena}}` (status is a string: idle/open/resolved, per docs/ARENA-API.md) | PASS: Chrome showed ok, mongo and er all true, program J83q…, arena round 8 open with endTs 1789257000, equal to the ER Arena |
| API-02 | `GET /api/arena` | H | 200 ArenaSnapshot; `round.roundId` equals the ER arena `current.id`; all arrays present | PASS: Chrome showed round 8 (ER current.id 8); round, recentRounds (8…1), trades, points, closes, traders, chat and cheers all present |
| API-03 | `GET /api/rounds?limit=5` | H | ≤5 rounds newest first; each resolved one has outcome YES/NO matching the on-chain history | PASS: rounds 8…4 newest first; outcomes 7 YES, 6 NO, 5 NO, 4 YES with strike and close equal to the ER Arena history |
| API-04 | `GET /api/trades?roundId=<n>` | H | Every `TradeExecuted` of that round; `sig` resolvable on the ER; amounts in USD | PASS: round 4 returns 6 trades, matching the on-chain trade count of 6; every sig is finalized on the ER with a Buy/Sell instruction; amounts in USD |
| API-05 | `GET /api/points`, `/api/closes`, `/api/settlements`, `/api/cheers` | H | Shapes per docs/ARENA-API.md; data matches on-chain events | PASS: points ascending every 5s; round-4 closes show tp +1.104787 and sl -1.862707; settlements carry bonus 1.104787 and 1.862707 with sigs 3ubUoks…/5tZKJEm… (same as TEST-RUN-CHAIN); cheers 3acK2K49… randomness 4a55…2cb5 |
| API-06 | `GET /api/chat?limit=50` | H | Ascending, ≤50, persisted across a service restart | PASS: ascending and ≤50. A message written before the Railway deploy was served after boot, so it persisted across the restart; it was a service-agent test record and has been removed |
| API-07 | `POST /api/auth/nonce` bad wallet | H | 400 `{error}` | PASS: `verify-live-api.ts` got 400 wallet: Invalid wallet address |
| API-08 | auth verify: good signature / bad signature / reused nonce | H | token / 401 / 401 | PASS: `verify-live-api.ts` got a token for a good signature, 401 Invalid signature for a bad one, 401 for a reused nonce |
| API-09 | `POST /api/profile` without token; invalid name; valid | H | 401; 400; 200 ProfileDto, then `GET /api/profile/:wallet` returns it | PASS: 401 without token, 400 invalid name, 200 ProfileDto, and GET /api/profile/:wallet returns the same name |
| API-10 | `POST /api/faucet` without token; funded wallet; unfunded wallet; repeat | H | 401; `{skipped:true}`; `{signature}` whose transfer of 0.02 SOL lands on devnet; 429 | PASS: 401 without token; unfunded wallet got 20000000 lamports (tx XwZceLQD…, balance 0.02 SOL on devnet); funded repeat skipped:true; after draining below 0.01, repeat got 429 wallet limit |
| API-11 | CORS | H | Vercel origin gets `access-control-allow-origin`; a foreign origin doesn't | PASS: https://rogs-arena.vercel.app gets access-control-allow-origin on GET /api/arena (200) and on the POST /api/profile preflight (204, Content-Type and Authorization allowed); https://evil.example.com gets no allow-origin |
| API-12 | Errors | H | Unknown route → 404 JSON `{error}`; no stack traces anywhere | PASS: 404 Not found, 405 Method not allowed, 400 Invalid JSON body; no stack traces in any error body |

## 3. Realtime WebSocket, indexer, keeper

| ID | Item | Method | Correct means | Status |
|---|---|---|---|---|
| WS-01 | hello → snapshot | B/H | First frame is `snapshot` with the same round as `/api/arena` | PASS (H): `verify-live-ws.ts` got snapshot as the first frame after hello, with round 9 equal to /api/arena round 9. The browser half is re-checked in UI-01 |
| WS-02 | presence online count | B | A second tab raises `online` by 1 within 2s and drops it within 50s after closing | PASS (headless Chrome, local production build against live Railway): online went 3 -> 4 when a second tab loaded (under 1s) and 4 -> 3 about 1.0s after it closed |
| WS-03 | chat authenticated | B | Sent message is broadcast to both tabs and present in `/api/chat` after reload | H half PASS: broadcast reached the sender and a second socket, then persisted to /api/chat · B PASS (headless Chrome, local build 608d513): a guest typed and sent "ws chat check 01:05:33"; the other visitor’s tab received the chat frame 270 ms later and rendered it, the sender rendered it, /api/chat stored it (EzKj…HLVz), and it was still shown after reloading the viewer |
| WS-04 | chat unauthenticated / too long / flood | H | `error` frame each; nothing persisted | PASS: `verify-live-ws.ts` got error frames for unauthenticated chat, unauthenticated heart, invalid JSON, a message before hello, 281 chars, bpm 400, and a second message within 1s. The flooded message was not broadcast, and only the one valid message was persisted (removed after the run) |
| WS-05 | trade broadcast | B | An ER trade appears as a `trade` frame within 5s, and on the market chart and leaderboard | PASS (headless Chrome, local production build 608d513 against live Railway): a second visitor bought on the ER and the watching tab received trade frames 26vk3CJQ…, 4RFx8w4o…, 4QsAHfWE… 394 ms, 1127 ms and 364 ms after the on-chain trade time; the market chart and leaderboard showed the new rows |
| WS-06 | round broadcast | B | At rollover a `round` frame arrives; the UI countdown resets to the new round | PASS (headless Chrome, local production build): at 01:05:01 the watching tab received round frames for round 23 resolved NO and round 24 opened; the header countdown read 04:50 at 01:05:10, the history strip marked 06:30 N and moved the current slot to 06:35 (ws-after-roll.png) |
| WS-07 | reconnect | B | Server restart or going offline → client reconnects and receives a fresh snapshot; no unhandled error | PASS (headless Chrome on https://rogs-arena.vercel.app, real Railway restart via the Railway API at 01:00:22): the arena WebSocket closed at 01:00:22, the client opened a new socket at 01:00:23, and a fresh snapshot for round 23 arrived 2s after the close. 0 unhandled page errors; the one transient "Failed to load resource: net::ERR_FAILED" was a request that fell inside the ~2s restart window |
| IDX-01 | Indexer persistence | H | Trades from an E2E run exist in Mongo with matching sigs | PASS: round-4 trades and settlements in Mongo carry the exact sigs of the on-chain ability run (3ubUoks…, 5tZKJEm…); backfill on Railway read 986 txs, 45 events, 0 failures |
| IDX-02 | Backfill after restart | H | After redeploy, trades made while down appear in `/api/trades` | PASS: `verify-keeper-settle.ts` bought while the Railway indexer was disabled (tx 48jQpd4p…, /health indexer.enabled=false); after INDEXER_ENABLED=true the backfill returned it in /api/trades 64s later |
| KPR-01 | Keeper settle fan-out | H | After resolution every trader of the round is settled within 30s without a manual `settle_player` | PASS: `verify-keeper-settle.ts` round 10 rolled at 1789257601; the position settled 1s later in tx 3fvVSGxE…, signed by keeper 59o1…; the player never sent settle_player |
| KPR-02 | Keeper cheers | H | A Cheers win gets `request_cheers` from the keeper and `CheersPaid` follows | PASS (`verify-keeper-cheers.ts`, live Railway keeper, round 23): P2 bought YES with Cheers (2Wxy93J4…) and sold into P1’s $90 move for 6.572087 on a 5 USD cost; no settle_player or request_cheers was sent by the script. After the crank resolved round 23, the keeper 59o1… settled P2 (PE9kz5ce…) and sent request_cheers (uvTQ8pGy…). The VRF callback 5LC56dLU… paid 10 recent traders 1 USD each, including P1 (randomness b186e4ee…), and /api/cheers indexed it |
| KPR-03 | Keeper watchdog | H | Logs show the crank rolled the round and no duplicate roll tx | PASS: Railway log at 23:50:06 shows Crank rolled: round 8 -> round 9, last_roll_ts 1789257001 (1s after end); keeper.lastRollSig is null, so no duplicate roll tx |

## 4. Web app (Vercel) — every screen and component

| ID | Component / flow | Method | Correct means | Status |
|---|---|---|---|---|
| UI-01 | First load | B | Preload screen, then the full grid; title "Rogs Arena \| BTC …"; 0 console errors; 0 failed requests | PASS: on https://rogs-arena.vercel.app the preload screen (mascot + "Loading art") shows at 400 ms, and by 900 ms the full grid is up with title "Rogs Arena / BTC - $77,256.16" (headless Chrome preload-400.png, preload-900.png). Claude-in-Chrome load: 62 requests all 200/304, 0 console errors or warnings. Fresh-guest sessions on the latest build also log 0 console errors and 0 failed requests (verify-fixes UI-01) |
| UI-02 | Header countdown | B | Counts down to the on-chain `end_ts` (±1s); rolls to the next round at 0 | PASS: at 00:13:24 UTC the header showed 01:36, which is on-chain end_ts 1789258500 (00:15:00) minus now |
| UI-03 | History timeline | B | 5-minute slots show Y/N matching on-chain round outcomes | PASS: all 12 resolved slots match /api/rounds and the ER history (IST slot = round start): 05:35 N=r12 NO, 05:30 Y=r11 YES, 05:25 N=r10 NO, 05:20 Y=r9, 05:15 Y=r8, 05:10 Y=r7, 05:05 N=r6, 05:00 N=r5, 04:55 Y=r4, 04:50 N=r3, 04:45 N=r2, 04:40 Y=r1; the current 05:40 slot has no letter |
| UI-04 | BTC chart + tab title | B | Live BTC price from the MagicBlock oracle (±0.5% of the feed read), updating ≥1/s; opening-price line equals the round strike | PASS: the headless-Chrome render shows a live BTC line, and the dashed opening line label $77,259.37 equals the round-14 strike 7725937… (77,259.37). The title price $77,259.50-.62 was within 0.001% of oracle 77,259.576 (publish 1789258744) and updates every read. Charts render in real Chrome: 61 rAF/s, both canvases painted |
| UI-05 | Market chart | B | UP/DOWN odds line from real points; trade markers appear for real trades | PASS (headless Chrome, round 21 with 4 real trades by 5BdLWgRR…, AX4yCGrr…, C3HP…): the market chart drew the UP 49.9% / DOWN 50.1% line from indexed points, with trader avatar markers on the line at the trade times (backend-down-loaded.png); the leaderboard listed the same traders. Empty rounds draw only the 50% line (UI-06) |
| UI-06 | Empty states | B | A round with no trades shows empty chart/leaderboard copy, not blank or fake data | PASS: at the start of rounds 15 and 16 (0 trades on chain) the leaderboard read "Waiting for positions…" and the market chart drew the flat 50.0%/50.0% line from indexed chain points (source: chain) with no trade markers. A fresh load with no messages showed the empty chat panel with the placeholder only. No blank panels, no invented data |
| UI-07 | Status panel | B | Latency is the real MagicBlock ER RTT (non-constant, ms); FPS is real; online count equals WS `online` | PASS: headless Chrome showed "ER RTT 160 MS / 60 FPS", then 146 ms / 59 FPS across 8s (median of real getSlot calls, requestAnimationFrame FPS). Chrome showed "3 traders" while /api/arena online = 3. The hidden Claude-in-Chrome window shows "—" by design because sampling pauses |
| UI-08 | Guest setup | B | "Play as guest" → faucet SOL → init+delegate → session → chips. The island ends at ready with 250.00 chips; every tx is real | PASS: clicking PLAY AS GUEST in Chrome created guest FTBkigFu…VmbEH. On devnet: faucet transfer 2nFFzjT4 (00:15:19), InitPlayer+DelegatePlayer 4RyJL3z7 (00:15:20), CreateSessionV2 5kdUvZNc (00:15:22). ER Player balance 250, joined; island showed 0.0136 SOL · 250 chips; 0 console errors |
| UI-09 | Wallet connect (Phantom/Solflare) | B | Wallet Standard modal lists installed wallets; connecting uses that pubkey | Modal PASS / connect UNTESTABLE. Headless Chrome: Connect wallet opened the Wallet Standard modal (no extensions, so it showed "You’ll need a wallet on Solana to continue") and the island switched to CONNECTING ("Choose Phantom, Solflare or Backpack, or play with a devnet guest wallet"); 0 console errors, 0 failed requests. UNTESTABLE: approving a connection needs the owner to click approve in their Phantom/Solflare extension, and the Claude-in-Chrome window is occluded (0 rAF), so the modal never mounts there |
| UI-10 | Buy UP / DOWN | B | Island shows the fill, chips decrease by the amount, position shows shares, latency ms shown, ER explorer link opens | PASS (local production build a4dc203 against live Railway + devnet, headless Chrome): "Bought 9.8 YES for $5.00 in 275 ms on the MagicBlock ER." links to solscan.io/tx/5rAe3JAX…?customUrl=devnet-as.magicblock.app; the earlier Chrome run showed chips -5, 9.8 UP and fee payer = session key at fee 0 (CH-10 B) |
| UI-11 | Amount stepper and limits | B | Min 2 enforced; above balance or above 100 shows the program error text, no crash | PASS: in Chrome, Decrease stops at 2 (button disabled). Increase to 102, then Buy UP shows "The largest trade is $100.00." (the program MAX_TRADE), with no transaction, no crash and 0 console errors. The above-balance case cannot be reached with 244.9 chips and a $100 cap; the program rejection "Not enough chips" is covered by CH-11 |
| UI-12 | Take profit / stop loss (sell) | B | Sells all shares, chips increase by proceeds, close shows on the leaderboard as tp/sl | PASS for the sell: TP on UP sold all 9.780446 YES; ER balance 245 -> 249.9005 (proceeds 4.9005); position 0 shares; close indexed as sl -0.0995; leaderboard row shows SL; exactly 1 BUY + 1 SELL, so the overlap fix holds in production. Defect found: the button said TP and the island PnL showed +$0.01, but the executed exit realized -0.0995 (run log). Re-verified after fix 8901757 (local production build 608d513): right after a buy the island shows "Stop loss on UP" with a red PnL, and the sell realized $4.90 for $5.00 cost |
| UI-13 | Ability card flip + drag to island | B | Card flips; dropped with a position → on-chain `attach_ability` succeeds; the card is consumed; a second card is rejected with the program message | PASS (local production build, headless Chrome, guest 5BdLWgRR…): after a buy, dragging Double onto the trading island gave "Double price attached to your round position in 239 ms on the MagicBlock ER."; a second card (Protect) gave "Could not attach Protect loss: An ability card is already attached to this position" and returned to the rack |
| UI-14 | Ability parked, then trade | B | Card dropped with no position → next buy carries the ability (`TradeExecuted.ability` = card id) | PASS (local production build 608d513, headless Chrome, guest EPDpTaJA…): in a fresh round with no position, dragging Calm pulse onto the trading island parked it; the next Buy UP reported "Bought 9.8 YES for $5.00 in 272 ms on the MagicBlock ER. Calm pulse rides on this position." |
| UI-15 | Round end settlement in the UI | B | After rollover the position settles (keeper or claim); chips credited; ability result message is exact | PASS (local production build 608d513, headless Chrome, guest 4Q3F6kPa…): held 7.3 YES into round 23 (resolved NO). 3s after the end the island showed "Settled 1 position: paid $0.00, P/L -$5.10." linked to the keeper’s settle tx 5832jkre…; /api/settlements holds the same row (payout 0, profit -5.10). No Claim was pressed |
| UI-16 | Leaderboard | B | Rows for real traders this round with shares/avg price/PnL. At rollover the previous board stays up, marked frozen (original rizz-club hold, `advanceLeaderboardHold`), until the new round’s first trade replaces it with a new epoch | PASS (local production build 608d513, headless Chrome, round 23 resolved NO): the held board shows resolved values, not marks. trcZ… 148.2 UP @ 60.7¢ -$90.00; DVNN… 15.5 DOWN @ 32.3¢ +$10.50; FZyE… 7.5 UP @ 66.8¢ -$5.00; J7tf… closed TP +$1.57 kept its realized value (verify-fixes-board.png). Earlier live runs showed real traders’ shares, avg price and PnL during the round, and the hold until the next round’s first trade |
| UI-17 | Chat | B | Needs a wallet; sending persists and broadcasts; placeholder correct when signed out; 280-char limit | PASS: signed out, the placeholder reads "Connect wallet to chat"; signed in, "What’s that". Sent "gm arena, verifying chat on devnet" in Chrome; it rendered as rogsjudge and /api/chat holds it (t 1789259578775). Input maxLength 280 (server rejects 281, WS-04). 0 console errors |
| UI-18 | Display name | B | Edit name → saved via `/api/profile`, shown in chat/leaderboard after reload | PASS: Edit name -> "rogsjudge" -> POST /api/profile 200; GET /api/profile/FTBkigFu… returns displayName rogsjudge; after a full reload the island and leaderboard still show rogsjudge; 0 console errors |
| UI-19 | Progress badges | B | Values equal on-chain Player stats (trades, streak, calm wins, day trades) | PASS: Trade Masters 2/100, Streak Climber 0/5, Steal Heart 0/70, Day Trader 2/20 equal the ER Player FTBkigFu… tradesTotal 2, winStreak 0, calmWins 0, dayTrades 2 |
| UI-20 | Heart rate (Web Bluetooth) | B | Real wearable bpm shown and reported on-chain | UNTESTABLE: needs a physical Bluetooth LE heart-rate strap paired through the Web Bluetooth device chooser (a user gesture on the owner’s machine); no such device is available to this run. The on-chain half (report_heart 80 bpm stored, 20 bpm rejected, Calm pulse bonus paid at max_bpm 80) is verified in CH-17 and CH-19 |
| UI-21 | Refresh mid-flow | B | Reload during setup or after a buy restores state from chain (no duplicate init, same session) | PASS after a buy: reloading at 00:21 restored the guest, session and name from storage and chain (249.9005 chips = ER balance). Base history for FTBkigFu… has exactly one InitPlayer+DelegatePlayer and one CreateSessionV2, so the reload did no duplicate init |
| UI-22 | Double-click buy | B | Exactly one trade tx per click-burst (button disabled while pending) | PASS (headless Chrome): a double-click on Buy DOWN produced one trade; the Trade Masters counter went 1 -> 2 and guest EEaRS5fz… has tradesTotal 2 on the ER (1 YES buy + exactly 1 NO buy); re-run on the local production build: Trade Masters 1 -> 2 again |
| UI-23 | Backend down | B | With the Railway API unreachable, chat/leaderboard show a readable error; trading on the ER still works | PASS (local production build 58ad4b7, headless Chrome). The outage was real at the network level: a second browser mapped the service host to a closed port, and fetch /health was unreachable. Guest set up while the service was up; then the page rendered with 0 unhandled errors, the leaderboard showed "Could not load trades.", chat read "Chat is offline. Reconnecting to the arena…" and blocked sending, the island loaded 250 chips from chain, and Buy UP succeeded on the ER in 163 ms. The console shows only Chrome’s own ERR_CONNECTION_REFUSED logs for the refused service requests, which come with the outage |
| UI-24 | Proof page `/proof` | B | Live delegation status (router), crank last roll, oracle price and age, last VRF cheers, last commit, program and explorer links — all real | PASS (local production build 608d513, headless Chrome, checked against independent reads): /proof HTTP 200. Router: delegated yes, rollup devnet-as (independent router call isDelegated=true). Crank task 5441772889676668, open round #23 (matches /api/arena), last roll 6:30:01, round #22 resolved NO with the roll tx linked. BTC/USD $77,246.89 published 3s earlier. Last VRF callback 3acK2K49… linked with randomness 4a5544…. Commits counter 2 with the Solana snapshot at round #13. Program and accounts link to the explorer. 0 console errors, 0 failed requests. Not yet on Vercel production (deploys stuck) |
| UI-25 | Responsive | B | 390px and 1440px widths: no horizontal overflow, controls reachable | PASS (local production build 58ad4b7, headless Chrome): at 390x844, 1024x768 and 1440x900 there is no horizontal overflow (scrollWidth = viewport), charts paint, 58-61 FPS, 0 console errors, 0 failed requests. Under 1440px the desktop layout is CSS-zoomed to fit (desktop proportions kept, every control on screen). There is no dedicated phone layout |
| UI-26 | No mocks anywhere | S | `grep -ri "mock\|stub\|fake\|dummy\|placeholder\|lorem"` in apps/, packages/, programs/ → only legitimate uses (test doubles, input placeholder attrs), each justified | PASS: the grep over apps/web, apps/arena, packages and programs (ts, tsx, rs; excluding node_modules, .next, target) matched only `stub` in section-history.tsx (the length in px of the history path segment, not a test stub) and "No mocks" doc comments in the verification scripts. No mock, fake, dummy or sample data anywhere |

## 5. Deploy and integration

| ID | Item | Correct means | Status |
|---|---|---|---|
| DEP-01 | Railway deploy | Build succeeds, `/health` ok, logs show indexer subscribed and keeper running | PASS: the Dockerfile build succeeded (Railpack had detected Rust; fixed). Boot log shows mongo connected, indexer watching the program, keeper started; /health ok |
| DEP-02 | Atlas from Railway | `/health.mongo=true` and writes visible in Atlas | PASS: /health mongo:true from Railway; probe user, session and faucet writes were read back through the API and then removed |
| DEP-03 | Vercel deploy | Production build succeeds from apps/web with workspace packages | PASS: https://rogs-arena.vercel.app READY (dpl_2NrjTfYJv5NT323ysH2EP5rr9REX), built from apps/web with workspace packages; all 10 NEXT_PUBLIC vars match env.ts. Caveat: later CLI deploys stuck in UNKNOWN on Vercel since 00:24 UTC, so production still serves that build; commits after it are verified on a local production build against the same live services |
| DEP-04 | GitHub | `main` contains all code; no secrets committed (`git grep` for key material and the Mongo URI returns nothing) | PASS: git grep over tracked files finds no Mongo URI, password fragment, PEM key or byte-array keypair; full history (git log -p --all) has 0 hits; 0 tracked .env files (only .env.example). Keypairs stay in ~/.config/solana |

---

## 6. Nine coin markets and the Rogs brand (added 2026-09-13 04:30 UTC)

| ID | Item | Method | Correct means | Status |
|---|---|---|---|---|
| MK-00 | Program upgrade | S | On-chain ProgramData bytes equal the local multi-market build (`solana program dump` + cmp); existing BTC arena keeps rolling on the ER after the upgrade | PASS: upgrade landed in slot 497545076; `solana program dump` equals target/deploy/rogs_arena.so byte for byte (674,680 bytes); BTC kept rolling on the ER after it (round #69 resolved DOWN, rolled 1 s after end, 04:55 UTC) |
| MK-01 | Every market delegated | S | `check-markets.ts`: for BTC, ETH, SOL, BNB, XRP, DOGE, SUI, AVAX, LINK the base owner is `DELeGG…`, the ER owner is the program, router isDelegated=true | PASS (04:50 UTC): all 9 arenas have base owner DELeGG…, ER owner J83qUB…, router isDelegated=true (check-markets.ts). New markets bootstrapped 04:47–04:50 UTC; ETH first roll hit InvalidWritableAccount until the ER registered the delegation, rerun succeeded |
| MK-02 | Market config on-chain | S | Each arena stores its market id and its own MagicBlock oracle feed | PASS: each arena stores its market id 0–8 and the oracle PDA of its Pyth Lazer id (1, 2, 6, 15, 14, 13, 11, 18, 19); SDK test markets.test.ts re-derives every feed PDA |
| MK-03 | Round open per market | S | Each market has an open round with a strike and a feed updated ≤ 30 s ago on the ER | PASS: every market had an open round with a strike and an oracle update ≤1 s old (e.g. LINK round #1 id 8796093022209 strike 11.49079802, oracle 11.491 age 0 s) |
| MK-04 | Crank per market | S | `check-markets.ts watch`: the MagicBlock crank rolls every market's round ≤ 30 s after end with no manual roll_round | PASS (04:55 UTC, no manual roll_round): BTC #69→#70 1 s, ETH 0 s, SOL 1 s, BNB 1 s, XRP 0 s, DOGE 0 s, SUI 1 s, AVAX 1 s, LINK 0 s after end; each history holds the resolved round with its close price |
| MK-05 | Round ids never collide | S+U | Non-BTC round ids are `market·2^40 + n`; a player holding positions in two markets settles each against its own arena | PASS (05:24 UTC, test-market-guards.ts, each case hits its intended program path): stranger initialize_market → "Signer is not allowed to act for this account"; market 0 under ["arena",[0]] → "Invalid arena configuration"; re-init SOL → System "account … already in use" (SDK describeLogs now reports it instead of "Program error 0x0"); stranger schedule_round_crank / fund_treasury on SOL → Unauthorized; a delegated Player passed as the arena → "Account discriminator did not match what was expected"; state unchanged in every case. First run FAILED 3 checks whose rejections came from a different path than intended — scripts fixed and re-run |
| MK-06 | Service markets API | H | `/api/markets` lists 9 markets with `available` and the current round; `/api/arena?market=SOL` etc. are scoped; old clients without `market` still get BTC | PASS (production Railway, 05:24 UTC): /api/markets → 9 rows all available with namespaced round ids (BTC r74, ETH 1099511627782 … LINK 8796093022214); /api/arena?market=SOL → market SOL snapshot; /api/rounds?market=ETH → only ETH rounds; /api/trades and /api/settlements?market=ETH&roundId=1099511627781 → the 4 trades and 3 settlements of the Fair Cheers run, all market ETH; ?market=sol and ?market=PEPE → 400 "market must be one of BTC, ETH, …"; /api/arena with no market → BTC |
| MK-07 | WS market switch | W | `hello` with `market` and `{type:"market"}` return that market's snapshot; broadcast DTOs carry `market` | PASS (production wss): hello {market:ETH} → snapshot market ETH; {type:market, market:SOL} → snapshot market SOL; {type:market, market:PEPE} → error frame "market must be one of …"; next broadcast was a point frame carrying market BNB |
| MK-08 | Keeper per market | H | Keeper backs up rolls, settles and commits for each market; settlements land for positions in non-BTC markets | PASS (production keeper logs): boot lists all 9 markets live; crank rolls observed for all 9 at 05:10 and 05:15 UTC; settled ETH round 5 positions of 8jZrS3/GYmQ6R (payout 148.238266) and BTC round 72 of 8eDMMB; committed the BTC arena after round 72 (7/9); boot migration stamped market on legacy rounds 9, trades 1, points 115 |
| MK-09 | Web coin selector | B | Selecting SOL/ETH changes the island label, tab title, chart colour and price, history, leaderboard; URL `?market=` and reload keep it; 0 console errors; no overflow at 390 px | PASS (Claude in Chrome, local production build against Railway, 06:17 UTC): ?market=SOL → title "Rogs Arena | SOL - $101.74", island label SOL, price $101.74, rounds/arena/points/trades/closes all requested with market=SOL (round id 2199023255570), 200; opening the board and picking ETH → title "Rogs Arena | ETH - $2,522.10", label ETH, URL ?market=ETH, ETH-scoped requests 200; reloading / with no param restored the stored market into the URL; 0 console messages with tracking on; 390 px no horizontal overflow, bottom-sheet board and SOL switch verified in headless Chrome (final-390-selector.png, final-390-sol.png) |
| MK-10 | Trade a non-BTC market | B | Guest buys and sells on SOL from the UI; the ER tx goes to the SOL arena; the position settles after the round | PASS (Claude in Chrome on the local production build + on-chain, 06:16–06:20 UTC): a fresh guest (DOM click on PLAY AS GUEST; faucet funded 0.0136 SOL) joined the rollup, switched to SOL via the board, clicked Buy UP → "Bought 9.8 YES for $5.00 in 292 ms on the MagicBlock ER." with an explorer link; island shows "9.8 UP @ 51.1¢". ER tx 5e53xJng… err null, writes the SOL arena J4SQ3h… (BTC arena absent), fee payer is the session key, TradeExecuted; API lists the trade with market SOL. After round #18 resolved DOWN the Railway keeper settled it (3NfiAWzT…, PositionSettled payout 0, profit -5), /api/settlements?market=SOL has the row. 0 console messages; ER requests 200 |
| MK-11 | Proof page | B | `/proof` shows delegation, crank, oracle and round for all 9 markets | PASS (Claude in Chrome, local production build against Railway, 06:13 UTC): /proof lists all 9 coins with arena, router "delegated", crank task id, oracle price with age ≤ 15 s and the current round; BTC cards show delegation (router devnet-as, owner DELeGG…), crank last roll, oracle strike, the keeper's VRF Cheers callback 5utGZh… paying 7NRuYy… $1.00, commits 8 with the Solana snapshot at round #85; 16/16 requests 200 (router, ER, /api/markets); 0 console messages with tracking on before load |
| BR-01 | Header brand | B | Header marquee reads ROGS; the island shows the Rog frog mascot instead of the cat; no "rizz" text anywhere in the UI | PASS (Claude in Chrome, local production build, 06:22 UTC): no "rizz" in page text or any alt/title/aria attribute, no cat; header marquee renders 40 ROGS words; island mascot image loaded (alt "Rog, the Rogs Arena mascot", naturalWidth 128); footer placeholders replaced (re-verify after rebuild); headless screenshots final-1440-btc-top.png and final-390-top.png show the frog and spaced ROGS marquee |
| BR-02 | App icon | B | Tab icon is the Rog mascot (`/icon.png` 512 px) | PASS: <link rel=icon> points to /icon.png, served 200 image/png, 111,494 bytes with a PNG signature; the file is the Rog mascot resized to 512 px from docs/brand/rogs-mascot-source.png |
| BR-03 | Mascot provenance | D | Prompt saved in `docs/brand/PROMPTS.md`, source image in `docs/brand/rogs-mascot-source.png` | PASS: generated in ChatGPT (conversation "Create mascot logo", 1254×1254 transparent PNG), prompt and source saved |

## 7. MagicBlock depth additions (added 2026-09-13 05:00 UTC)

| ID | Item | Method | Correct means | Status |
|---|---|---|---|---|
| MG-01 | Fair Cheers v2 | U+S | `request_cheers` is rejected with "Every recent trader except the winner must be a Cheers candidate" unless every distinct recent trader other than the winner is passed; the full list is accepted and the VRF callback pays (unit: `cheers_tests`) | PASS (devnet, ETH round #5, verify-fair-cheers.ts): settle_player + request_cheers with P1 only (of 2 recent traders) rejected with "Every recent trader except the winner must be a Cheers candidate"; the complete list accepted (372cfSaV…) and the VRF callback paid both candidates +1 USD. Unit: cheers_tests. Railway keeper now sends the complete set (keeper-rules test) |
| MG-02 | VRF seed not caller-controlled | U | The VRF seed is sha256(caller_seed, winner, round id, slot, ts): changing any input changes it and it never equals the caller seed (unit: `vrf_seed_mixes_in_state_the_caller_cannot_choose`) | PASS (unit): vrf_seed_mixes_in_state_the_caller_cannot_choose — seed differs from the caller seed and changes with slot, round and winner; request_cheers passes cheers_seed(...) to VRF (deployed in upgrade #2) |
| MG-03 | Badges via Magic Action | S | `commit_player_badges` on the ER commits the Player; the post-commit action `record_badges` updates the owner's BadgeRecord on Solana to the ER Player's stats (`verify-badge-action.ts`) | PASS (after upgrade #3, verify-badge-action.ts): commit_player_badges on the ER (5GHEvQNX…) → post-commit action record_badges ran on Solana and set BadgeRecord updates 0 → 1, trades 1 = ER 1, badges 0 = ER 0. Run 1 FAILED (missing source_program slot) — fixed in the program |
| MG-04 | Action-only handler | S | `record_badges` sent directly by a wallet is rejected (escrow PDA must sign); BadgeRecord unchanged | PASS (after upgrade #3): record_badges sent directly by the owner wallet with the escrow unsigned is rejected on-chain with "A signer constraint was violated"; BadgeRecord updates stayed 0 |
| MG-05 | Commit receipts | S | `GetCommitmentSignature` from `@magicblock-labs/ephemeral-rollups-sdk` resolves the base-layer commit of the ER tx, and that base tx succeeded and ran RecordBadges | PASS (after upgrade #3): GetCommitmentSignature (@magicblock-labs/ephemeral-rollups-sdk) resolved base tx 3g9psLHt… for the ER commit; err null and "Instruction: RecordBadges" in its logs |
| MG-06 | Program upgrade #2 | S | On-chain bytecode equals the build with Fair Cheers v2 and the Magic Action; all 9 markets keep rolling afterwards | PASS: upgrade #2 landed in slot 497551540, `solana program dump` equals the build byte for byte (718,192 bytes); check-markets watch afterwards: all 9 markets rolled 0–1 s after end (BTC #72→#73, ETH–LINK #4→#5) |
| MG-07 | Keeper Cheers after v2 | H | The Railway keeper requests Cheers with the complete candidate list and the VRF callback pays | PASS (06:08 UTC, XRP round 4398046511113, verify-keeper-cheers.ts): no settle or request from the script; the Railway keeper (59o1Mk…) settled P2 (5A2hKpXo…) and sent request_cheers (WoLerxar…) with the complete Fair Cheers v2 set; the VRF callback (5utGZhqQ…) paid P1 +1 USD, randomness 5daee9eb…; /api/cheers indexed the CheersPaid row |
| MG-08 | Upgrade #3 (Magic Action fix, size-optimized build) | S | On-chain bytecode equals the opt-level "s" build of the fixed program; `anchor build` from the repo reproduces that exact binary; markets keep rolling | PASS: landed in slot 497556520, dump equals the 592,488-byte opt-level "s" build; `anchor build` from the repo is byte-identical; after it all 9 markets rolled 0–1 s after end (BTC #75→#76, ETH–LINK #7→#8), market guards 6/6 and Fair Cheers (BNB) re-passed, E2E 20/20 |
| MG-09 | Save badges to Solana from the UI | B | In the badges panel a joined player clicks "Save to Solana": the record is created on Solana if missing (wallet-signed base tx), commit_player_badges goes to the ER, the Magic Action updates BadgeRecord on Solana, the row shows "On Solana: N badges · best streak S · K saves" with working rollup and Solana explorer links; the button is disabled before joining; 0 console errors, 0 failed requests | PASS (Claude in Chrome on the local production build + on-chain, 06:17–06:19 UTC): guest 49mMEV… clicked "Save to Solana" in the badges panel → row showed "Saved by a MagicBlock Magic Action" with rollup and Solana links; Solana tx 5fLeDBcg… err null and ran RecordBadges; BadgeRecord updates 1. After a SOL trade, a second save went "Waiting for the Magic Action on Solana…" → "2 saves" in 4 s; Solana tx 2dc5c2oe… ran RecordBadges; BadgeRecord updates 2, trades 1 = ER player trades 1 (fresh committed state). 0 console messages; every base RPC and ER request 200 |

## Run log

Findings and fixes are appended here per run: item ID, observed result, root cause, fix commit, re-run result.

### Run 1 — findings so far (2026-09-13, before the app deploy)

| Item | Observed | Root cause | Fix | Re-run |
|---|---|---|---|---|
| CH-20 | The VRF callback paid the candidate +1.000000 USD, but `parseArenaEvents` returned no `CheersPaid` for the callback tx `3acK2K49…` or `CheersRequested` for `3z18MVog…` | Anchor `EventParser` loses the invoke stack around CPIs. The callback runs this program at depth 2 under the VRF program, and `request_cheers` logs after an inner VRF invoke | `packages/arena-sdk/src/events.ts` now tracks the invoke stack and decodes `Program data:` only when this program is on top. A regression test uses both real devnet log arrays | PASS: the live tx `3acK2K49…` decodes `CheersPaid{recipients:[BymPz…], amountEach:1000000, randomness:32 bytes}`; SDK tests 13/13 |
| E2E report | `docs/E2E-RUN.md` step 19 showed **PASS** with the detail "not exercised this run", so a skipped VRF step was counted as passed (21/21) | `e2e-devnet.ts` recorded the skipped branch as `ok: true` and had no skipped state | `Step.skipped` gives the verdict `NOT EXERCISED`, and the summary counts only exercised steps. The row is corrected, and VRF evidence comes from the deterministic run: request `3z18MVog…`, callback `3acK2K49b37rgwVbSXFBdYmk7p937iNzHmrvMBpktJLsfTSZrTazMsQqTivjbkH5kE9VcCJztK4cGJupKTxvVbtV` | tsc 0 errors, SDK 13/13 |
| Evidence docs | Re-running `e2e-devnet.ts` or `test-guards.ts` would delete the sections that `commit-arena.ts`, `e2e-cheers-vrf.ts` and `test-abilities.ts` append (commit_arena, Cheers VRF, ability bonuses) | Both writers called `writeFileSync` on the whole file | Both now replace only their own table and keep every `## ` section after it | tsc 0 errors |
| MAGICBLOCK-AUDIT | The audit reported VRF Cheers and the router call as unverified, with zero router call sites | The audit read `E2E-RUN.md` step 19 before the deterministic VRF run, and it missed `scripts/check-delegation.ts` | Rows 8 and 12, §1.1 and §1.3 #6 now carry the signatures and call site | — |
| CH (undelegate) | `undelegate_player` had no live proof | No script called it | `scripts/verify-undelegate.ts` runs undelegate (ER `3sCuC1UPpgtuqoxGcBRBMeHQvYjYusYCvyXCHViTMcEYhjh2Sfahidqs1c1jWfetxBWUC4FKr5S8DcKx5ewTgaCm`), then checks the base owner is the program and the balance is 250.000000 on base. Router status goes true → false, then re-delegation (`2HBvpN1PwDy6wi9qKbh81XGCQgDhMQi1ikYvVr28podWyTQmnUJBmL1EDXcXh4M5szKJmx96hiSxeXJCzCxqUshR`) | PASS 8/8 |
| KPR commit cap | The keeper counted commits only in Mongo (starting at 0), while the Arena already had 1 on-chain commit, so it could hit MagicBlock's 10 plain commits per delegation early | Commit count was not read from chain | `maybeCommit` uses `max(mongo, arena.commits)` against `MAX_COMMITS = 9` | tsc 0 errors, keeper-rules 4/4 |
| API tests | `schemas.test.ts` "wallet addresses … keep their case" failed 1 run in 6 | The test lowercased a random key, which sometimes decodes to another valid 32-byte key | Uses a fixed key containing `L` (its lowercase `l` is not base58) plus an invalid-character case | see next run |
| DEP-01 | The first `railway up` failed. Railpack detected Rust from the root `rust-toolchain.toml`, ran `cargo build --release`, and never installed Bun | Auto-detection keys on the repo root, which holds the Anchor workspace | Added `apps/arena/Dockerfile` (oven/bun 1.3.11, repo-root context, exec-form CMD for SIGTERM), set `RAILWAY_DOCKERFILE_PATH`, added `.dockerignore`. Start command is `bun run apps/arena/src/index.ts` | Deploy SUCCESS; boot log shows mongo, indexer, keeper |
| IDX-02 (attempt 1) | The buy landed while the service was already back up, so the attempt was recorded as a FAIL instead of a backfill check | A Railway restart only takes ~2s (SIGTERM 23:55:15.97, boot 23:55:17.90); the flag went down at 23:55:24 | The script now counts the indexer as down when `/health.indexer.enabled` is false. The re-test runs with `INDEXER_ENABLED=false` during the trade | re-test pending |
| KPR crank lifetime | The crank task ran 200,000 × 2s and would expire around 2026-09-17 14:18 UTC; the keeper only logged the stall | Nothing re-scheduled an expired task | The keeper sends `schedule_round_crank` with a fresh task id after 2 consecutive stalled rounds, with a 10-minute backoff and task-id parity with the SDK | 46/46 service tests, tsc 0; live path UNTESTABLE until the current task expires |
| DEP-03 | The first `vercel --prod` upload aborted; the retry hit the free-tier limit of 5000 uploaded files per 24h | Without a `.vercelignore` the CLI uploaded the repo root, including target (1.9 GB) and node_modules | `.vercelignore` allow-list (apps/web, packages, lockfile), deployed with `--archive=tgz` (25 MB) | READY |
| UI-10 | Buy worked (status "Bought 9.8 YES for $5.00 in 229 ms on the MagicBlock ER.", chips -5, 9.8 UP shown) but there was no ER explorer link to open | `use-trading` sets `status.explorerUrl`, but `pane-trading-zone` renders only `status.message` | Render the status line as a link to the ER explorer when `explorerUrl` is set | PASS on the local production build 608d513: the status line links to solscan.io/tx/2P27c9e2…?customUrl=devnet-as |
| UI-12 | The TP/SL label and island PnL used the mark price (+$0.01), so the button said TP while the executed sell realized -$0.0995 (1% fee + price impact) | `floatingProfitsForTrader` values shares at the mark instead of the executable exit | Value the player’s own exit with the SDK `quoteSell` on the live pools (fee included) | PASS on the local production build 608d513: "Stop loss on UP" with a red PnL right after a buy; the sell realized $4.90 on a $5.00 cost |
| UI-15 | The keeper settled the round-15 position 3s after the end (payout 0, P/L -5, tx 5W4YVfMg…), but the island showed no settlement result | The island only built a settlement message on a manual Claim | `use-trading` watches for positions leaving the Player account and reports the indexed PositionSettled rows (with ER explorer link); Claim marks its rounds so they are not reported twice | PASS on the local production build 608d513: "Settled 1 position: paid $0.00, P/L -$5.10." 3s after round 23 ended, linked to keeper tx 5832jkre… |
| UI-16 | After round 15 resolved NO, the frozen board still showed rogsjudge +$0.01 for 9.8 YES that were worth $0 | The hold froze the last mark-based values | While frozen, `use-leaderboard` fetches the held round’s outcome and `resolveHeldItems` values open shares at $1/$0 | PASS on the local production build 608d513: the round-23 board showed -$90.00 / +$10.50 / -$5.00 resolved values |
| UI-01 (fresh guest) | Headless Chrome logged `Failed to load resource: 404` for GET /api/profile/<new guest> | The service answered 404 for a wallet that never saved a name, and the browser logs every 404 | GET /api/profile/:wallet returns 200 with displayName null for unknown wallets (ARENA-API.md updated) | PASS: after the Railway deploy the profile lookup returns 200; fresh-guest runs log 0 console errors |
| UI-13/14 procedure | Dragging a card onto the island in its information view did nothing, in both Chrome and headless Chrome | By design, cards apply only while the island is in its trading zone (`canApplyAbilityOnIsland`); the drag shows a trading-zone hint | Not a defect: the flow opens Trade before dragging | UI-13 and UI-14 PASS in headless Chrome with Trade opened first |
| UI-13 | Headless Chrome: attach PASS ("Double price attached to your round position in 159 ms on the MagicBlock ER."). A second card (Protect) dropped on that position stayed parked ("Protect loss ×") with no message; the next Buy DOWN then carried it and failed, so the UI-22 re-run counted 0 trades | `useReleaseAppliedOnMarketEnd` returned early when the position already held a different card | The island now sends `attach_ability` anyway; the program rejects it with its message and `clearApplied` returns the card to the rack | PASS on the local production build 608d513: "Could not attach Protect loss: An ability card is already attached to this position"; UI-22 re-run then counted exactly 1 trade (1 -> 2) |
| UI-01 (fresh guest) re-run | After the Railway deploy, GET /api/profile/EEaRS5fz… (never named) returns 200 {displayName:null, createdAt:0, updatedAt:0} | — | — | PASS: a fresh-guest session on the local production build logged 0 console errors and 0 failed requests (verify-fixes UI-01) |
| UI-25 (390/1024) | At zoomed widths Chrome logged 2 net::ERR_ABORTED requests (an ER POST and /api/rounds) during load; there were no visible errors | `ViewportFit` rendered children bare on the first client render, then wrapped them after measuring, so the whole app remounted and effects aborted their first requests | The wrapper element is always rendered (display: contents when not zoomed), so the tree never remounts | PASS on 58ad4b7: the 390x844 run shows failed: [] and consoleErrors: [] |
| WS-03 re-run | During the chat check the viewer tab logged "WebSocket connection … failed: WebSocket is closed before the connection is established." | A sign-in in another tab (shared localStorage) or a quick unload made `setAuth`/`shutdown` call close() on a socket still CONNECTING | `closeSocket` (lib/socket-close.ts) detaches handlers and closes a CONNECTING socket only once it opens; 3 unit tests | PASS on 58ad4b7: the WS-03 re-run shows errors: [] |
| UI-23 re-run | The first two outage runs did not take the service down (URL blocking and request interception let data through), so they proved nothing | Browser-level blocking was bypassed | Real outage instead: a second Chrome with --host-resolver-rules maps the service host to 127.0.0.1:9 (connection refused) | PASS on 58ad4b7 (see UI-23) |

### Run 2 — full top-to-bottom re-run (2026-09-13 04:10 UTC onward)

| Scope | Result | Evidence |
|---|---|---|
| CH-24 Rust unit tests | PASS | cargo test -p rogs-arena --lib: 22 passed, 0 failed |
| CH-25 SDK tests | PASS | bun test: 13 pass, 0 fail |
| Service unit tests | PASS after fix | First run 40 pass / 2 fail: the beforeAll hook connecting to Atlas timed out at bun's 5 s default under load, then afterAll crashed on an undefined db. Fixed in faucet.live, auth and limits tests: 30 s hook timeout and a guarded drop. Two isolated re-runs: 46 pass, 0 fail |
| Web tsc + unit tests | PASS | tsc 0 errors; 226 pass, 0 fail |
| API-07..API-12 (verify-live-api.ts) | PASS | all 17 checks, faucet tx 3CgJbMAS…, probe documents removed |
| WS-01, WS-03 (HTTP half), WS-04 (verify-live-ws.ts) | PASS | round 61 snapshot equals /api/arena; all error frames; nothing persisted from errors |

### Run 3 — multi-market + MagicBlock depth (2026-09-13 04:30 UTC onward)

| Item | Finding | Root cause | Fix | Re-test |
|---|---|---|---|---|
| Upgrade #1 | `solana program deploy` failed with "9 write transactions failed" | api.devnet.solana.com rate limits during the buffer write | Resumable `--buffer` keypair, upgrade sent through rpc.magicblock.app | PASS: bytecode matches (MK-00) |
| MK-01 bootstrap | ETH first roll_round rejected with InvalidWritableAccount right after delegation | The ER had not yet registered the new delegation | bootstrap-markets.ts retries only that error | PASS: all 9 markets delegated and rolling |
| E2E P1 settle / CH-19 | Balance delta 0 | The live keeper settles first; the scripts measured the balance just before their own (no-op) settle | scripts/lib/settlement.ts measures from the pre-roll balance and reads PositionSettled from whichever tx settled | PASS: e2e-devnet 20/20 and test-abilities CH-19 both pass (Run 3) |
| MK-05 (run 1) | 3 checks rejected through a different path than intended | Test built market 0 with the legacy PDA; System error surfaced as "Program error 0x0"; the posed account didn't exist on the ER | Script aims each check at its path; SDK describeLogs reports non-program failures with their own log reason (unit test) | PASS (run 2) |
| MG-03/MG-05 | Magic Action failed on Solana | Missing `source_program` slot in the #[action] context | Added and pinned to this program | pending upgrade #3 |
| MG-04 (run 1) | Rejection came from web3.js, not the program | Escrow marked signer in the IDL | Script sends it unsigned | pending re-run |
| Keeper settle log (MK-08) | Keeper logged "Settled E3ctkY… ETH round 5" for tx 28hQQSHG… | The position was already settled 4 slots earlier (372cfSaV…, script's settle + Cheers); the keeper's batched read was stale and it logs success without checking for PositionSettled | Keeper only logs "Settled" when the tx emitted PositionSettled for that owner and round; otherwise logs that another tx settled it | PASS (live, 06:08 UTC): redeployed keeper logged "Settled 8bC9Rw8z… for XRP round 9" for 5A2hKpXo…, the same tx the keeper-Cheers script found carrying PositionSettled, then "Requested VRF cheers … with 1 candidates"; 73/73 service tests incl. the settledIn unit test |
| Upgrade #3 budget | Default build needs a 3.65 SOL buffer; wallets pooled 3.53 SOL and the airdrop is rate-limited | Program grew to 719 KB with Magic Actions and Fair Cheers | Size-optimized release profile (opt-level "s", 592 KB, 3.01 SOL buffer), pinned in Cargo.toml so builds reproduce the deployed binary | PASS: bytecode matches |
| E2E after keeper-race fix | P1 settle_player balance delta was 0 in Run 2 | The keeper settled first; the script measured just before its own no-op settle | settleObserved measures from the pre-roll balance and reads PositionSettled from whichever tx settled | PASS: e2e-devnet 20/20 (05:25 UTC round 75: P1 payout 9.552616 + Calm bonus 10, P2 payout 0; commit_player shows trades 1 / balance 264.552616 on Solana) |
| MG-03/MG-05 after upgrade #3 | — | — | — | PASS: Magic Action wrote BadgeRecord on Solana; commit receipt resolved |
| MK-05 / MG-01 regression after upgrade #3 | — | — | — | PASS: market guards 6/6; Fair Cheers on BNB curated list rejected, full list paid 2 candidates |
| Keeper redeploy | Keeper settle confirmation deployed to Railway | — | — | 73/73 service tests before deploy |
| Undelegate lifecycle on upgrade #3 bytecode | — | — | — | PASS (05:58 UTC): init + delegate, claim 250 USD on the ER, undelegate_player (32DuaHNn…) returns the Player to the program on Solana with 250 USD, router isDelegated false, re-delegation keeps 250 USD on the ER |
| Live API + WS on the multi-market service | — | — | — | PASS: verify-live-api 17/17 (API-07/08/09/10/12, faucet 0.02 SOL tx 2fhu6GmR…, 429 on repeat) and verify-live-ws 11/11 (WS-01, WS-03, WS-04) against production |
| Program guards on upgrade #3 bytecode | — | — | — | PASS: test-guards 17/17 — claim cooldown, session-key isolation, min/max/balance limits, slippage, oversell, ability rules incl. last-30 s lock, heart range, idempotent settle, authority-only fund/commit, last-5 s trade lock |
| UI-01 on the multi-market build | — | — | — | PASS (Claude in Chrome, reload with tracking on): title "Rogs Arena | BTC - $77,291.84", URL ?market=BTC, ROGS marquee, badge row rendered; 54/54 requests 200/304 including /api/markets and market-scoped rounds, arena, points, trades, closes; 0 console errors or warnings |
| Footer placeholders | The about footer showed a literal `{s}{d}` and "Version 0.1.12-oven" (rizz-club leftovers) | Hardcoded strings in features/about/index.tsx | Left cell is a "MagicBlock proof" link to /proof; right cell shows "Rogs Arena v" + package.json version | PASS (Claude in Chrome after rebuild, 06:31 UTC): "MagicBlock proof" links to /proof, footer reads "Rogs Arena v0.1.0", no `{s}{d}` or "oven" anywhere in the page |
| Claude-in-Chrome click flows | PLAY AS GUEST click had no effect | The automation window is occluded: visibilityState hidden, 0 animation frames/s, element rects at negative coordinates, so coordinate clicks don't land | Click-driven flows are driven by DOM click() or verified in headless Chrome with a real viewport; DOM, network and console checks stay in Claude in Chrome | see MK-09, MK-10, MG-09 |
| Chat from the UI on the multi-market build | — | — | — | PASS (Claude in Chrome, rebuilt production build, 06:31 UTC): signed-in guest 49mM…Sf75 typed in the chat input (placeholder "What's that") and submitted; input cleared, the message rendered in the page and is stored in /api/chat under 49mM…Sf75; 10/10 Railway requests 200; 0 console messages with tracking on before the reload |
| UI-23 service outage on the multi-market build | — | — | — | PASS (headless Chrome, rebuilt production build, 06:39 UTC): with the Railway host mapped to a dead port in that browser (/health unreachable), the page rendered with 0 unhandled errors, the leaderboard showed "Could not load trades.", chat showed "Chat is offline. Reconnecting to the arena…", the player still loaded from the chain (0.0236 SOL · 250 chips), and a buy on the ER succeeded ("Bought 9.8 YES for $5.00 in 258 ms"). The only console error was the induced WebSocket connection refusal |
| ws-chat (browser) run 1 on the new build | Crashed: the sender guest's balance line did not appear within 90 s after PLAY AS GUEST | Unknown: the script printed no page state (four browsers were setting up guests against devnet at the same time) | Script now reports the island text on a setup timeout; re-run alone with an unused funded guest | PASS when run alone (06:44 UTC, fresh guest B8hj…4opG): viewer tab received the chat frame 287 ms after send, both tabs rendered it, /api/chat stored it, still shown after a reload, errors []. Open question: the four concurrent stalls (verify-fixes, flow, ws-chat, ws-browser) all used guests whose Player already existed; returning-guest setup is being reproduced alone |
| Layout after the badge row (headless, rebuilt build) | — | — | — | PASS: layout-check 1440×900 min gap 13 px, 390×844 min gap 68 px, no overlapping panels |
| markets-check on the rebuilt production build (port 3000) | 10/13 PASS: BTC default (title, URL, label, stored), board lists 9 coins with prices, SOL and ETH via selector, bare URL restores ETH, back to BTC, no overflow at 1440, 390 SOL from URL with no overflow, 390 board as a full-width sheet, proof lists 9 rows with oracle prices; consoleErrors [] and wsErrors [] for the whole run | 3 FAIL: "chart drawn in coin colour" needs > 50 px and measured BTC 43, SOL 42, ETH 44 right after each switch (later samples 319 and 573 px) | Investigating whether the price chart starts nearly empty after a switch (no history) or the check samples too early | pending |
| DEP-03 Vercel (Run 3) | Every deploy to the `rogs-arena` project since 00:24 UTC sat in UNKNOWN with no build, including a 06:40 retry of commit 71aa700 | Project-specific: the same team's `masked` project deployed READY in 2 s at the same time, so the account and CLI work | New Vercel project `rogs-arena-app`: `vercel build --prod` in a clean git worktree of the verified commit, then `vercel deploy --prebuilt --prod --project rogs-arena-app` with the 10 public env values | PASS (build): deployment rogs-arena-b7gjjgsqg READY in 8 s at https://rogs-arena-app.vercel.app. PASS (07:00 UTC): Railway CORS_ORIGIN includes the origin, the WebSocket accepts it, and the fix build 566d698 is deployed (c43s0zrrh) as the production alias. Claude in Chrome on the live site: 0 console errors, requests 200. Headless returning guest 7yNtK5LK… joined on production (SIGNING IN → CREATING SESSION → "0.0208 SOL (devnet) · 255.9395 chips" in 4 s, 0 console errors, 0 failed requests) |
| Returning-guest setup (headless suite) | verify-fixes, flow, ws-chat and ws-browser all timed out waiting for the balance line; only fresh guests passed | Diagnosed alone (existing-guest-setup.mjs, guest 0): SIGNING IN → JOINING ROLLUP → CREATING SESSION → "Could not claim starting chips. Balance is too high to use the faucet". usePlayer().refresh() read the render-time owner, which was null because the guest wallet connects after PLAY AS GUEST, so setup saw no joined player and re-claimed chips for a player holding 245 | refresh(target) reads by address; setup passes the owner; regression test trade-setup-returning.test.ts; commit on main | PASS (06:51 UTC, fix commit 566d698 built in an isolated worktree, headless Chrome): guest 0 (returning, 245 chips) went SIGNING IN → CREATING SESSION → "0.0194 SOL (devnet) · 245.9005 chips" in 4 s with no chips claim; 0 console errors, 0 failed requests. Stalled suites re-running on this build |
| Live site https://rogs-arena-app.vercel.app (Claude in Chrome + WS probe, 07:00 UTC) | Round-scoped points/trades/closes calls were absent at a 10 s read | Timing: they follow the realtime snapshot, which arrived later; they went out and returned 200 by 58 s | — | PASS: origin rogs-arena-app.vercel.app, ?market=SOL, title "Rogs Arena · SOL - $101.21", SOL label, price and change label, market odds, badges row, "MagicBlock proof" footer link, "Rogs Arena v0.1.0", no rizz or {s}{d}; 32 requests (static assets, MagicBlock ER RPC, Railway /api/markets, rounds, arena, points, trades, closes) all 200 or in flight, none failed; 0 console messages over 112 s with tracking on before load; chat not offline, presence "3 traders". Railway CORS_ORIGIN now includes the new origin (allow-origin header confirmed 35 s after the change), and a WebSocket opened with Origin https://rogs-arena-app.vercel.app received a SOL snapshot in 1,002 ms |
| WS-05 / WS-06 on the fix build (headless, returning guest 3) | — | — | — | PASS (06:53–06:55 UTC, local production build of 566d698): the returning guest joined without stalling and its trade reached a second visitor as a trade frame in 790 ms (WS-05); at the 06:55:00 rollover the watcher received round frames for resolved and newly opened rounds across markets, e.g. LINK 8796093022233 → 8796093022234 and XRP 4398046511129 → 4398046511130 (WS-06) |
| Core-flow regression on the fix build (headless, returning guests 0 and 1, 06:52–07:05 UTC) | Every product step passed. The only FAILs were the scripts' setup assertions, which expected exactly "· 250 chips" (a fresh player) while these returning guests hold 245.9005 and 244.4376 chips | Test defect: a hardcoded fresh-player balance in verify-fixes.mjs and flow.mjs | Assertions now accept any on-chain chips balance line; both scripts re-running | Passed this run: UI-10 status links to the ER explorer. UI-12 "Stop loss on UP" with red PnL, and the sell realized the loss as labelled. Position held into round 94. UI-15 island reported "Settled 1 position: paid $0.00, P/L -$5.10." with explorer link. UI-16 frozen board shows the resolved -$5.10. UI-01 0 console errors, 0 failed requests. UI-13 buy, attach Double, second card rejected. UI-22 double-click sends one trade (Trade Masters 4 → 5). UI-14 parked Calm pulse rides on the buy |
| Core-flow regression re-run on the fix build (06:52 fix, 07:06–07:20 UTC) | — | — | — | PASS: verify-fixes (returning guest 0): guest ready 0.0166 SOL · 240.801 chips, UI-10 explorer link, UI-12 "Stop loss on UP" + loss realized as labelled, position held into round 97, UI-15 "Settled 1 position: paid $0.00, P/L -$5.10.", UI-16 frozen board, UI-01 0 console errors / 0 failed requests. flow (returning guest 1): guest setup 0.0194 SOL · 248.9985 chips, UI-13 buy / attach Double / second card rejected, UI-22 double-click → one trade (Trade Masters 7 → 8), UI-14 parked Calm pulse rides on the buy |
| Loading screen brand (reported by the owner) | The preload screen still showed the rizz cat and a RIZZ wordmark pattern | Inline SVG art in components/preload-screen.tsx that text-based brand checks could not detect | Frog mascot image + ROGS wordmark pattern | PASS (headless production build): 350 ms desktop and mobile show the frog over ROGS rows with "Syncing live data", no rizz text (preload-fixed-desktop-350.png, preload-fixed-mobile-350.png); a contact sheet of all 28 non-avatar public images shows no other cat or RIZZ art |


### Run 4 — recording readiness on localhost (2026-09-13 07:40 UTC onward)

Stack: local service on :8787 (Mongo, indexer, price sampler; the Railway keeper settles on the same devnet program), local production web build on :3000, and five demo bots (`apps/arena/scripts/demo-activity.ts`, "Rogbot …" wallets) trading and chatting through the real product paths. Nothing was deployed.

| Item | Finding | Root cause | Fix | Re-test |
|---|---|---|---|---|
| Coin logos next to the price and in the selector | Both BTC logo `<img>` tags reported `complete: false`, `naturalWidth 0` in Claude in Chrome although `/coins/btc.svg` returned 200 image/svg+xml | `next/image` defaults to `loading="lazy"` and the automation window is a hidden tab, so the lazy images never started | `CoinLogo` loads eagerly (tiny, above the fold, part of the price readout) | PASS: Claude in Chrome both BTC logos complete, naturalWidth 24; after switching to SOL all 11 logos (island, price, 9 board tiles) loaded. Headless 1440 BTC/SOL/ETH/DOGE/LINK and 390 BTC/SOL: every logo loaded and visible, 0 errors, no horizontal overflow |
| Traders presence with the demo bots | "1 trader" while five bots traded and chatted | The bots sent `hello` only; presence expires after 45 s without a frame (the web client pings every 15 s) | Bots send `{type:'presence'}` every 15 s and reconnect on close | PASS: snapshot online 6 (5 Rogbots + guest), page shows "7 traders" on every market and at 390 px |
| Demo bot slippage | Some bot trades were skipped with "Price moved beyond the allowed slippage" | 3 bps between quote and execution while five bots trade the same pools | 100 bps, like a normal UI setting | PASS: 07:41–08:14 UTC 137 buys, 32 partial sells, 17 settles, 62 with ability cards, 38 chat lines, 0 skipped, 0 failed |
| Bot trades are real on-chain trades | — | — | — | PASS: 12/12 indexed trades sampled across BTC/SOL/ETH/BNB confirmed on the MagicBlock ER (`getSignatureStatuses` + `getTransaction`, each with a Buy instruction, err null) |
| Guest trade from the UI (Claude in Chrome, guest 49mM…Sf75) | — | — | — | PASS: Trade → Buy UP: "Bought 9.6 YES for $5.00 in 190 ms on the MagicBlock ER."; ER Player balance 251 → 246 with a 9.64433 YES position in BTC round 108, indexed with the same signature (ER err null). "Stop loss on UP": "Sold 9.6 YES for $4.90 in 181 ms on the MagicBlock ER." then "Settled 1 position: paid $0.00, P/L -$0.10."; ER balance 250.90, 0 active positions, settlement for round 108 indexed |
| Chat from the UI and live chat | — | — | — | PASS: guest message rendered and stored in /api/chat under 49mM…Sf75; a new Rogbot message appeared in the open tab 3 s later with no reload; a whitespace-only message is not sent (send button disabled, nothing stored) |
| Coin switch in the real browser | — | — | — | PASS: board lists 9 coins, each with logo, live price and round; SOL tile sets ?market=SOL, title "Rogs Arena \| SOL - $100.58", island "SOL market, switch coin"; /api/prices, rounds, arena, points, trades, closes for SOL all 200; 0 console errors |
| Ability card reveal | — | — | — | PASS: "Reveal Double price" toggles to "Hide Double price" (aria-pressed true) |
| Proof page | — | — | — | PASS (headless 1440): 9 coin rows delegated with crank task ids, oracle prices seconds old and current rounds; ER delegation, crank and VRF panels populated |
| Save to Solana row over the status bar | At 1440×900, 1280×800, 1440×1000 and 1920×1080 the badge row (33 px) drew over "7 traders" in the footer | The progress list used `h-full`, so it took the whole panel height and pushed the badge row outside the panel (panel 273 px, content 306 px) | List sizes to its content | PASS (rebuilt production build, headless): badge row inside its panel and above the status bar at 1440×900 (803–836 vs panel bottom 844, status from 852), 1440×1000, 1920×1080 and 1280×800 |
| Unit tests | — | — | — | PASS: web 269/269, arena service 83/83, tsc and eslint clean |
| Save to Solana from the UI (Magic Action) on the recording stack | — | — | — | PASS (Claude in Chrome, guest 49mM…Sf75, 08:17 UTC): the badges row went "Waiting for the Magic Action on Solana…" → "Saved by a MagicBlock Magic Action · rollup tx · Solana tx" in 4 s, saves 2 → 3; 0 console errors. On Solana the program-owned BadgeRecord 5n2q71…KfM reads updates 3, tradesTotal 2, updatedTs 1789287447, written by tx 58jBp3cm… (08:17:27 UTC, err null) invoked through the delegation program DELeGG… |
| Round-history panel in Claude in Chrome | Its text and cells are not readable through innerText in the hidden automation tab | Automation-window limitation, not an app defect (same class as the lazy image finding) | Covered by the headless suite instead (UI-16 frozen board) | see flow/verify-fixes re-run |
| Core flow on the recording build (headless flow.mjs, guest Hgic…Fa, 08:29–08:35 UTC) | — | — | — | PASS: guest setup 0.0194 SOL · 253.7804 chips; UI-13 buy "Bought 9.3 YES for $5.00 in 236 ms on the MagicBlock ER.", Double attached in 294 ms, second card rejected ("An ability card is already attached to this position"); UI-22 double-click Buy DOWN sends one trade (Trade Masters 3 → 4); UI-14 parked Calm pulse rides on the buy ("Bought 10.2 YES … Calm pulse rides on this position."); consoleErrors [] |
| Settlement and exits on the recording build (headless verify-fixes.mjs, guest F7Za…k7, 08:35–08:40 UTC) | — | — | — | PASS: guest ready 0.0222 SOL · 246 chips; UI-10 "Bought 9.9 YES for $5.00 in 306 ms on the MagicBlock ER." links to the ER explorer; UI-12 "Stop loss on UP" with red PnL and the sell realized the labelled loss ("Sold 9.9 YES for $4.90 in 183 ms"); position held into round 114; UI-15 keeper settlement in the island "Settled 1 position: paid $0.00, P/L -$5.10." with explorer link; UI-16 frozen board after resolution (outcome NO, -$5.10); UI-01 consoleErrors [] and failed [] |

### Run 5 — full demo flow with a real wallet in the Claude Browser pane (2026-09-13 09:08 UTC onward)

**Wallet.** Rogs Demo Wallet `FVrAiH7Q…kZg5`:
- A fresh devnet keypair, funded with 0.05 SOL from the deployer.
- Registered in the page through Wallet Standard by `apps/arena/scripts/demo-wallet.ts`.
- Its signer is bound to 127.0.0.1, refuses other origins, and logs each request. The key never enters the page.

**Stack.** The local stack from Run 4, with the five demo bots trading.

| Item | Finding | Root cause | Fix | Re-test |
|---|---|---|---|---|
| Connect wallet through the wallet-adapter modal | — | — | — | PASS: "Connect wallet" lists "Rogs Demo Wallet — Detected". The island went SIGNING IN → JOINING ROLLUP → CREATING SESSION → CLAIMING CHIPS → "0.0436 SOL (devnet) · 250 chips" in 13 s. Signer log: 1 signMessage ("Rogs Arena sign-in wallet: FVrA… nonce …"), then 2 Solana txs paid by the wallet: [compute-budget, rogs-arena] (3mfAFZRq…) and [compute-budget, Gum session] (tfKcYSUb…). On Solana the Player PDA aLbpWjaG… is owned by DELeGG… (delegated); ER balance 250 |
| Trade, card, exit, settlement with the wallet | — | — | — | PASS. Buy UP: "Bought 9.1 YES for $5.00 in 252 ms on the MagicBlock ER." (4ukdPQyR…, signed by the session key, not the wallet). Chat message stored and shown. Round end: keeper SettlePlayer 3vfGuBXD… "Settled 1 position: paid $0.00, P/L -$5.00." Double price dragged with real pointer input while no position was open, so it parked; the next buy read "Bought 9.3 YES … Double price rides on this position." (ability 1 on-chain). "Stop loss on UP": "Sold 9.3 YES for $4.90 in 298 ms" (Buy 49uTvEjv…, Sell 43GqX81j…, err null) |
| Coin switch, Save to Solana, proof page | — | — | — | PASS. SOL switch: URL, title, island, logos. Save to Solana: "Creating your badge record on Solana…" → "Waiting for the Magic Action on Solana…" → "Saved by a MagicBlock Magic Action" in 4 s. InitBadgeRecord 2myYhpNF… paid by the wallet; RecordBadges dda5QEYg… paid by the MagicBlock validator through DELeGG…; BadgeRecord 8YKckrmd… updates 1. Proof page: 9 delegated arenas, oracle, VRF Cheers, commits and economy panels |
| Price history after coin switches | Two console errors "Failed to load resource: net::ERR_EMPTY_RESPONSE" on /api/prices for ETH and DOGE after ~7 s. Six concurrent requests all got an empty reply after 8.7 s; the service log said "[Bun.serve]: request timed out after 10 seconds" | An hour of history is ~1,800 rows. Over the remote Atlas link one query took 2.8 s (default 101-row batches) and several at once passed Bun's 10 s idle timeout | The sampler keeps recent prices in memory (`PriceHistory`), preloads the last hour at boot, and /api/prices answers covered windows from it. The Mongo fallback reads in one batch; the HTTP idle timeout is 30 s. 5 new tests | PASS. Service preloaded 11,198 points in 7 s. Six concurrent 1 h requests: 200 in 3–5 ms each (was 0 bytes after 8.7 s); a 3 h window falls back to Mongo in 1.4 s. Browser pane through all 9 coins: every /api/prices 200 in 1–29 ms. Service suite 88/88, tsc clean |
| Arena snapshot latency (not fixed) | /api/arena takes 2.6–4.3 s and /api/rounds 1.7–3.3 s against the remote Atlas cluster | Several Mongo reads per snapshot over a high-latency link | None yet. Well under the new 30 s timeout; the video dwells ~5 s after a switch | — |
